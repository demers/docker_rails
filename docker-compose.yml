version: '2.1'

services:
  rails:
    build: .
    image: rails
    container_name: rails
    volumes:
      - "data_work:/home/ubuntu/work"
      - "/home/demers/share:/home/ubuntu/share"
    ports:
      - "22:22"
      - "3000:3000"
    network_mode: "host"

volumes:
  data_work:
